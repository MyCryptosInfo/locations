{
  "name": "mycryptos-locations",
  "version": "0.0.0",
  "private": true,
  "description": "MyCryptos · Locations",
  "main": "src/index.js",
  "scripts": {
    "lint-staged": "lint-staged",
    "standa": "standa --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "lint-staged"
  ],
  "lint-staged": {
    "*.{json,md}": "prettier --write",
    "*.{js}": [
      "prettier --write",
      "standa --fix"
    ]
  },
  "prettier": {
    "arrowParens": "always",
    "bracketSpacing": true,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "none"
  },
  "devDependencies": {
    "lint-staged": "^10.5.4",
    "pre-commit": "^1.2.2",
    "prettier": "^2.2.1",
    "standa": "^16.1.1"
  }
}
